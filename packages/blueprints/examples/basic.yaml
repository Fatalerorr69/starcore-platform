name: demo
version: "1.0"
resources:
  - name: web-vm
    provider: proxmox
    kind: vm
    config:
      disk: 120
      image: ubuntu-24.04
  - name: postgres
    provider: docker
    kind: container
    config:
      image: postgres:17
      volume: postgres-data
